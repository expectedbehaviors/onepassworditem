# Package the Helm chart and publish to GitHub Release + gh-pages.
# Uses expectedbehaviors/github-actions helm-publish (values_image_tag_key points at dummy key in values.yaml).

name: Helm chart publish

on:
  release:
    types: [published]
  workflow_run:
    workflows: ["Release on merge to main"]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag (e.g. v1.0.0). Default: latest release.'
        required: false

permissions:
  contents: read

jobs:
  package-and-upload:
    runs-on: ubuntu-latest
    if: github.event_name == 'release' || github.event_name == 'workflow_dispatch' || (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Package and upload Helm chart
        id: helm-publish
        uses: expectedbehaviors/github-actions/.github/actions/helm-publish@main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          chart_path: .
          chart_name: onepassworditem
          values_image_tag_key: _helm_publish.image.tag
          release_tag: ${{ github.event.inputs.release_tag }}
